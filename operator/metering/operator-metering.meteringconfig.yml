apiVersion: metering.openshift.io/v1
kind: MeteringConfig
metadata:
  name: operator-metering
  namespace: openshift-metering
spec:
  storage:
    type: hive
    hive:
      type: sharedPVC
      sharedPVC:
        claimName: metering-claim
        createPVC: true
        size: 5Gi
